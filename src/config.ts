// Configuration file
// If you are deploying to a static host and cannot set environment variables,
// you can paste your Gemini API Key here.
// WARNING: This key will be visible to anyone who views the source code of your site.
// It is recommended to restrict the key's usage to your domain in the Google AI Studio console.

export const CONFIG = {
  // Paste your key inside the quotes if environment variables are not working
  GEMINI_API_KEY: "", 
};
